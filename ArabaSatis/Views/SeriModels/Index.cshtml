@model IEnumerable<ArabaSatis.Models.SeriModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SeriAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MarkaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SeriAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MarkaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SeriId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SeriId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SeriId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
